# E2E Test Environment Configuration
# This file is used ONLY for running E2E tests locally and in CI/CD

# Database Configuration for E2E Tests
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=myapp_e2e
DB_DATABASE=myapp_e2e

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-e2e-tests-only
JWT_EXPIRES_IN=1d
REFRESH_TOKEN_SECRET=test-refresh-token-secret-for-e2e-tests-only
REFRESH_TOKEN_EXPIRES_IN=7d

# Environment
NODE_ENV=test
PORT=3000

# OAuth (not required for E2E tests, but needed for app initialization)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
